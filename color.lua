-- Fluent modificado com tema vermelho escuro (139, 0, 0)

-- (código original omitido por limite de espaço)
-- Substitua onde encontrar:
-- Theme.Colors.Accent = Color3.fromRGB(96, 205, 255)
-- por:
Theme.Colors.Accent = Color3.fromRGB(139, 0, 0)
-- Também troque:
-- any `Color3.fromRGB(96, 205, 255)` usados nos sliders, toggles, etc.
-- por `Color3.fromRGB(139, 0, 0)`
